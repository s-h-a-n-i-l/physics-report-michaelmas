Theory Guidance:

1. Key Equations:
- Solenoid field: $H = I * n_p / L_p$ (Eq 6.1)
- Measurement of I via V_x: $V_x = I * R_p$ (Eq 6.2)
- Induced EMF: $E_s = n_s * d(\phi)/dt$ (Eq 6.3)
- Integrator output: $V_y$ proportional to $B$ (Eq 6.5)
- Flux in sample: $\phi$ approx $B_{sample} * A_{sample}$ (Eq 6.6)
2. Concepts:
- Explain Hysteresis: Directional dependence, energy loss per cycle = Area of loop (integral H dB).
- Saturation: Explain that $\mu_r$ approaches 1 at saturation.
3. Conventions:
- Variables in italic (e.g., $B$, $H$), Units not in italic (e.g., $T$, $A/m$).
- Define all symbols before use.
